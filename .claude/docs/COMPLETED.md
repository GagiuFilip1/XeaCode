# Completed Work

Reverse-chronological log of shipped work. One-liners only — full detail belongs in plan documents and commit history.

- **2026-05-12 — Sub-phases 2.2 + 2.3 (combined) + Romanian removal** — Three new sections shipped together: `SelectedWork.tsx` (RSC shell) + `work/WorkRows.tsx` (Client leaf, stagger reveal, pipe-separated stack tag parsing), `Team.tsx` (pure RSC, fully static — stats row + 3 paragraphs + 4 hairline-separated principles), `Faq.tsx` (pure RSC, static `<dl>`, 7 Q/A, no JS). In the same pass: Romanian removed completely — deleted `messages/ro.json`, `src/components/layout/LocaleSwitcher.tsx`, `src/i18n/routing.ts`, `src/i18n/navigation.ts`, `src/proxy.ts`, the entire `src/app/[locale]/` directory; moved `[locale]/{layout,page}.tsx` up to `src/app/{layout,page}.tsx`; simplified `request.ts` to hardcode `locale="en"`; collapsed `sitemap.ts` to a single URL; stripped multi-locale exports from `site-config.ts` + `jsonld.ts`; swapped `@/i18n/navigation` Link import → `next/link` in Header + Footer. URLs now at `/` (no `/en/` prefix); build output `ƒ /`. Header nav: 7 entries (Services, Process, Work, Stack, Team, FAQ, Contact); LocaleSwitcher gone. 30 new message keys (Team: 14; FAQ: 16) drafted by Content Creator in one pass + 15 `[NEEDS USER INPUT]` Work tokens for user fill. Brand Guardian PASS, Code Reviewer APPROVED with 1 MINOR (stale `[locale]` doc refs in `theme-config.ts`, `ThemeProvider.tsx`, `README.md` — fixed inline). `bun run lint` + `bun run build` clean. Plan: `~/.claude/plans/phase-2-2-2-3-combined.md`.

- **2026-05-12 — Sub-phase 2.1 — Copy expansion to existing sections** — Tripled substantive copy across 5 existing sections (Hero, Services, Process, Tech, Contact) + added 11 new credibility-detail keys (`hero.trustStrip`, 4× `services.*.engagement`, 4× `process.*.deliverable`, `tech.scopeNote`, `contact.preformNote`). Bilingual EN + RO at parity (native-feeling RO, not translated). Velocity positioning landed via aggressive duration bands (4–8wk product builds vs. 12–20 industry standard) + Hero subtitle velocity line + AI-tooling hybrid mention in Tech subtitle only. Lead trust-strip number: `60+ years of senior engineering`. Wave 2 placement moves locked: trust strip ABOVE CTAs (not below), deliverable ABOVE step title (not below description), preform note rendered TWICE (header + submit-adjacent). NDA segment swapped for "walk away with the written plan" to serve the individual buyer. Brand Guardian + Code Reviewer signed off (1 RO metadata fix applied — "Studio Premium..." calque → "Studio independent de inginerie software"). `bun run lint` + `bun run build` clean. 5 custom agents used: Trend Researcher + SEO Specialist + Brand Guardian + Senior PM + Plan (Wave 1); SEO + Growth Hacker + Brand Guardian (Wave 2); Content Creator (bilingual drafting); Brand Guardian + Code Reviewer (pre-merge). Plan: `~/.claude/plans/phase-2-1-copy-expansion.md`. Spec: `~/.claude/plans/phase-2-content-credibility.md`.

- **2026-05-12 — Sub-phase 1.4 — SEO + perf + a11y (local-only — deploy deferred)** — Expanded metadata API (OG + Twitter + canonical + hreflang), `robots.ts`, `sitemap.ts`, `ProfessionalService` JSON-LD inline (React 19 OK for data-type scripts), 1200×630 placeholder OG image, skip-to-main link, `data-scroll-behavior` attribute. Final Lighthouse: desktop /en + /ro = 100/100/100/100; mobile /en = 95/100/100/100. LCP 0.6s desktop, CLS 0. Two a11y remediations: dropped Process step opacity fade (broke contrast for off-screen text) + dropped wordmark aria-label (mismatched visible text). 3 lessons captured. **Phase 1 is complete locally.** Deploy split off to future Sub-phase 1.5. Plan: `~/.claude/plans/phase-1-4-seo-perf-a11y.md`. Audit summary: `.claude/docs/audits/lighthouse-summary.md`.

- **2026-05-12 — Sub-phase 1.3 — Contact (mocked) + Footer + paw easter egg #2** — Mock API contract layer (`src/lib/api/{contracts/contact.ts, contact.mock.ts, contact.ts}`) typed for one-line swap to the future .NET 10 Web API. Contact section as 2-col grid (form left, details right at `lg+`, stacked mobile) with full 5-state form machine (idle/submitting/error/success/field-validation), hand-rolled validation, controlled inputs, ~1200ms mock latency + UUID success card with slide-in. ContactDetails RSC with 3 placeholder rows. Paw icon extracted to shared `src/components/icons/Paw.tsx` (used by Hero backdrop AND Footer); `PawDot.tsx` deleted. Footer Client Component with 3-col block (brand/quick-links/socials), bottom row with dynamic year + reactive theme indicator, centered easter-egg line with paw SVG #2. page.tsx wire-up complete — all 6 sections + footer live. Lint clean, build clean, dev clean. 3 hotfix-lessons captured before 1.3 started (cookie-backed theme, Client References, ProcessStep useScroll target). Plan: `~/.claude/plans/phase-1-3-contact-footer.md`.

- **2026-05-11 — Sub-phase 1.2 — Content sections (Services, Process, Tech Stack)** — Services 2×2 stagger-reveal grid with hover lift + Phosphor icons; Process sticky-scroll 4-step narrative with side progress indicator driven by `useScroll`/`useTransform`; Tech Stack twin counter-scrolling CSS-keyframes marquees with edge-fade mask and 16 placeholder tiles (real brand logos swap into `public/logos/` later). Page wired up: Hero → Services → Process → Tech Stack → Contact placeholder. Lint clean, build clean. 1 lesson: RSC→Client boundary can't pass function refs (refactored `logos.tsx` to serializable plain-data). 0 tests (deferred to 1.4). Plan: `~/.claude/plans/phase-1-2-content-sections.md`.

- **2026-05-11 — Sub-phase 1.1 — Foundation + Hero** — Next.js 16.2.6 + Bun + Tailwind v4 scaffold, EN+RO i18n via next-intl, dark-default theming via next-themes, sticky glass-blur Header with theme/locale toggles + mobile menu, asymmetric left-aligned Hero with split-text headline, magnetic CTAs (Framer `useMotionValue`), animated mesh-gradient + dot-grid backdrop, inline-SVG paw easter egg #1, full `prefers-reduced-motion` fallback. Lint clean, build clean, dev boots in 288ms. 0 tests (deferred to Sub-phase 1.4). Plan: `~/.claude/plans/phase-1-1-foundation-hero.md`.
